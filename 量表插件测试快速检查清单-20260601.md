# 📋 量表插件测试快速检查清单

**测试日期**: 2026-06-01  
**测试人员**: rich  
**插件版本**: scale-plugin.js v1.0.0  
**测试环境**: admin-legacy.html

---

## 🎯 测试目标

验证 `scale-plugin.js` 在实际页面环境中所有功能正常工作，包括：

1. 插件正确加载和初始化
2. 量表列表渲染正常
3. 量表编辑功能完整
4. 量表删除功能正常
5. 量表导出功能正常
6. 事件通信正常
7. 存储同步正确
8. 向后兼容性良好

---

## 📝 测试前准备

### 1. 环境检查

- [ ] 确认 `scale-plugin.js` 已放置在正确位置
  - 核心插件路径：`/mini-app-h5/backend/plugins/core/scale-plugin.js`
  - 可选插件路径：`/mini-app-h5/backend/plugins/optional/scale-plugin.js`
- [ ] 确认 `admin-legacy.html` 已正确引入核心模块
  - `plugin-base.js`
  - `dual-adapter.js`
  - `event-hub.js`
  - `plugin-loader.js`
- [ ] 确认浏览器控制台没有加载错误

### 2. 数据准备

- [ ] 确认 `scales-data.json` 文件存在且有数据
- [ ] 确认 `localStorage` 中有量表数据（键名：`psy_scales`）
- [ ] 准备测试用量表数据（至少3个量表）

---

## ✅ 测试清单

### 一、插件加载测试

#### 1.1 插件自动加载

- [ ] **操作步骤**：
  1. 打开 `admin-legacy.html`
  2. 按 F12 打开浏览器控制台
  3. 观察控制台输出
- [ ] **期望结果**：
  - 看到 `📋 scale-plugin.js v1.0.0 已加载`
  - 看到 `🚀 量表管理插件开始初始化...`
  - 看到 `✅ 量表管理插件初始化完成，共加载 X 个量表`
  - 看到 `✅ 量表管理全局函数已创建（向后兼容）`
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 1.2 插件手动加载（按需加载）

- [ ] **操作步骤**：
  1. 在控制台输入 `PluginLoader.load('scale', false)`
  2. 观察返回值
- [ ] **期望结果**：
  - 返回 Promise，解析为 ScalePlugin 实例
  - 控制台输出 `✅ 插件 scale 加载成功`
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 1.3 插件信息获取

- [ ] **操作步骤**：
  1. 在控制台输入 `PluginLoader.get('scale')`
  2. 输入 `PluginLoader.getPluginsInfo()`
- [ ] **期望结果**：
  - `get('scale')` 返回 ScalePlugin 实例
  - `getPluginsInfo()` 数组中包含 scale 插件信息
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 二、量表列表渲染测试

#### 2.1 初始加载渲染

- [ ] **操作步骤**：
  1. 打开页面，自动切换到"量表管理"页面
  2. 观察量表列表表格
- [ ] **期望结果**：
  - 表格显示所有量表数据
  - 每行的信息正确：名称、题目数、时长、完成人数、评分、更新时间、状态、NPC配置
  - 分页信息正确："共 X 条记录 / 全部 Y 个量表"
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 2.2 搜索筛选

- [ ] **操作步骤**：
  1. 在搜索框输入量表名称关键词
  2. 观察列表变化
- [ ] **期望结果**：
  - 列表实时筛选，只显示匹配的量表
  - 分页信息更新
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 2.3 分类筛选

- [ ] **操作步骤**：
  1. 点击分类下拉框，选择某个分类
  2. 观察列表变化
- [ ] **期望结果**：
  - 列表只显示选中分类的量表
  - 分页信息更新
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 2.4 状态筛选

- [ ] **操作步骤**：
  1. 点击状态下拉框，选择"已上架"或"已下架"
  2. 观察列表变化
- [ ] **期望结果**：
  - 列表只显示对应状态的量表
  - 分页信息更新
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 2.5 NPC配置筛选

- [ ] **操作步骤**：
  1. 点击 NPC 配置筛选下拉框，选择"已配置"或"未配置"
  2. 观察列表变化
- [ ] **期望结果**：
  - 列表只显示符合条件（NPC配置状态）的量表
  - 分页信息更新
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 2.6 排序功能

- [ ] **操作步骤**：
  1. 点击表头"评分"列
  2. 观察列表排序变化
  3. 再次点击"评分"列
- [ ] **期望结果**：
  - 第一次点击：按评分升序排列
  - 第二次点击：按评分降序排列
  - 排序图标更新（↑ 或 ↓）
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 2.7 拖拽排序模式

- [ ] **操作步骤**：
  1. 点击"拖拽排序"按钮
  2. 拖动某个量表行到新位置
  3. 再次点击"完成排序"按钮
- [ ] **期望结果**：
  - 进入拖拽模式：按钮变为"完成排序"，光标变为 grab
  - 可以拖动量表行调整顺序
  - 点击"完成排序"后，顺序保存，前端按新顺序显示
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 三、量表编辑功能测试

#### 3.1 新建量表

- [ ] **操作步骤**：
  1. 点击"新建量表"按钮
  2. 填写量表信息（名称、简称、编码、分类等）
  3. 添加题目（至少1道）
  4. 点击"保存"
- [ ] **期望结果**：
  - 弹窗正常打开，标题显示"新建量表"
  - 表单字段可以正常输入
  - 题目编辑器正常工作
  - 保存成功后，列表新增该量表
  - 显示成功提示
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 3.2 编辑量表

- [ ] **操作步骤**：
  1. 点击某个量表的"⋯"按钮
  2. 选择"编辑量表"
  3. 修改量表信息
  4. 点击"保存"
- [ ] **期望结果**：
  - 弹窗正常打开，标题显示"编辑量表：XXX"
  - 表单自动填充当前量表信息
  - 修改后保存成功，列表更新
  - 显示成功提示
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 3.3 题目编辑器

- [ ] **操作步骤**：
  1. 打开新建/编辑量表弹窗
  2. 添加题目，填写题目内容
  3. 选择预设选项（如"频率4选项"）
  4. 添加/删除选项
- [ ] **期望结果**：
  - 题目列表正常显示
  - 可以添加、删除、上移、下移题目
  - 选项编辑器正常工作
  - 预设选项功能正常
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 3.4 答题须知编辑器

- [ ] **操作步骤**：
  1. 打开新建/编辑量表弹窗
  2. 观察"答题须知"部分
  3. 添加/删除须知项
- [ ] **期望结果**：
  - 答题须知编辑器正常显示
  - 可以添加、删除、修改须知项
  - 保存后须知项正确存储
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 3.5 NPC配置

- [ ] **操作步骤**：
  1. 打开新建/编辑量表弹窗
  2. 填写 NPC 配置（咨询师ID、背景ID）
  3. 保存量表
- [ ] **期望结果**：
  - NPC 配置字段正常显示和保存
  - 保存后，`npcConfig` 字段正确存储
  - 列表中的 NPC 配置状态正确显示
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 四、量表删除功能测试

#### 4.1 删除确认弹窗

- [ ] **操作步骤**：
  1. 点击某个量表的"⋯"按钮
  2. 选择"删除量表"
- [ ] **期望结果**：
  - 弹出确认对话框，显示"确定要删除「XXX」吗？"
  - 点击"取消"，不删除
  - 点击"确定"，执行删除
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 4.2 删除执行

- [ ] **操作步骤**：
  1. 确认删除某个量表
- [ ] **期望结果**：
  - 量表从列表删除
  - 存储数据更新
  - 显示成功提示："量表已删除"
  - 触发 `scale-deleted` 事件
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 五、量表导出功能测试

#### 5.1 导出格式选择弹窗

- [ ] **操作步骤**：
  1. 点击某个量表的"⋯"按钮
  2. 选择"导出"
- [ ] **期望结果**：
  - 弹出格式选择弹窗
  - 显示两个按钮："JSON格式"和"CSV格式"
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 5.2 导出为 JSON

- [ ] **操作步骤**：
  1. 在格式选择弹窗中点击"JSON格式"
  2. 观察文件下载
- [ ] **期望结果**：
  - 下载文件：`量表编码_日期.json`
  - 文件内容正确：包含 `exportTime`、`version`、`scale` 字段
  - `scale` 字段包含完整的量表数据
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 5.3 导出为 CSV

- [ ] **操作步骤**：
  1. 在格式选择弹窗中点击"CSV格式"
  2. 观察文件下载
- [ ] **期望结果**：
  - 下载文件：`量表编码_日期_26fields.csv`
  - 文件内容正确：26字段格式，每行对应一道题目
  - 包含题目选项（动态列数）
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 5.4 批量导出

- [ ] **操作步骤**：
  1. 点击"导出全部"按钮
- [ ] **期望结果**：
  - 所有量表导出为一个 JSON 文件
  - 文件内容正确
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 六、上架/下架功能测试

#### 6.1 下架量表

- [ ] **操作步骤**：
  1. 找到一个"已上架"的量表
  2. 点击"⋯"按钮，选择"下架"
- [ ] **期望结果**：
  - 量表状态变为"已下架"
  - 状态标签更新为灰色
  - 显示成功提示："量图表已下架"
  - 触发 `scale-status-changed` 事件
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 6.2 上架量表

- [ ] **操作步骤**：
  1. 找到一个"已下架"的量表
  2. 点击"⋯"按钮，选择"上架"
- [ ] **期望结果**：
  - 量表状态变为"已上架"
  - 状态标签更新为绿色
  - 显示成功提示："量图表已上架"
  - 触发 `scale-status-changed` 事件
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 七、事件通信测试

#### 7.1 插件初始化事件

- [ ] **操作步骤**：
  1. 在控制台输入以下代码：
     ```javascript
     window.EventHub.on('scale-initialized', (data) => {
       console.log('✅ 收到 scale-initialized 事件:', data);
     });
     ```
  2. 刷新页面
- [ ] **期望结果**：
  - 页面加载完成后，控制台输出事件数据
  - 数据包含：`count`（量表数量）、`timestamp`
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 7.2 量表删除事件

- [ ] **操作步骤**：
  1. 在控制台输入以下代码：
     ```javascript
     window.EventHub.on('scale-deleted', (data) => {
       console.log('✅ 收到 scale-deleted 事件:', data);
     });
     ```
  2. 删除一个量表
- [ ] **期望结果**：
  - 删除成功后，控制台输出事件数据
  - 数据包含：`scaleId`、`scaleName`、`timestamp`
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 7.3 状态改变事件

- [ ] **操作步骤**：
  1. 在控制台输入以下代码：
     ```javascript
     window.EventHub.on('scale-status-changed', (data) => {
       console.log('✅ 收到 scale-status-changed 事件:', data);
     });
     ```
  2. 上架或下架一个量表
- [ ] **期望结果**：
  - 状态改变后，控制台输出事件数据
  - 数据包含：`scaleId`、`scaleName`、`status`、`timestamp`
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 八、存储同步测试

#### 8.1 保存到 localStorage

- [ ] **操作步骤**：
  1. 编辑某个量表并保存
  2. 在控制台输入 `localStorage.getItem('psy_scales')`
- [ ] **期望结果**：
  - `localStorage` 中的 `psy_scales` 键已更新
  - 数据是最新的量表列表（JSON 格式）
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 8.2 同步到前端存储

- [ ] **操作步骤**：
  1. 确保某个量表状态为"已上架"
  2. 在控制台输入 `localStorage.getItem('psy_scales_synced')`
- [ ] **期望结果**：
  - `localStorage` 中的 `psy_scales_synced` 键包含已上架的量表
  - 前端页面可以正确加载这些量表
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 8.3 从 localStorage 加载

- [ ] **操作步骤**：
  1. 刷新页面
  2. 观察量表列表
- [ ] **期望结果**：
  - 列表显示的数据与 `localStorage` 中的 `psy_scales` 一致
  - 修改、删除、添加操作后，刷新页面，变更仍然存在
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 九、向后兼容性测试

#### 9.1 全局函数存在

- [ ] **操作步骤**：
  1. 在控制台输入以下代码，检查全局函数是否存在：
     ```javascript
     console.log('loadScales:', typeof loadScales);
     console.log('saveScales:', typeof saveScales);
     console.log('renderScaleList:', typeof renderScaleList);
     console.log('openEditModal:', typeof openEditModal);
     console.log('confirmDelete:', typeof confirmDelete);
     console.log('toggleStatus:', typeof toggleStatus);
     console.log('exportSingleScale:', typeof exportSingleScale);
     ```
- [ ] **期望结果**：
  - 所有函数类型都是 `function`
  - 可以正常调用这些函数
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 9.2 全局函数功能

- [ ] **操作步骤**：
  1. 在控制台输入 `renderScaleList()`
  2. 观察量表列表是否重新渲染
- [ ] **期望结果**：
  - 列表重新渲染，显示最新数据
  - 没有错误信息
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 9.3 旧代码调用

- [ ] **操作步骤**：
  1. 在控制台输入 `loadScales()`
  2. 观察返回值
- [ ] **期望结果**：
  - 返回当前存储中的所有量表数组
  - 数据与 `localStorage` 中的一致
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

### 十、性能测试

#### 10.1 插件加载时间

- [ ] **操作步骤**：
  1. 打开控制台，清除缓存
  2. 刷新页面
  3. 使用 `performance.now()` 测量插件初始化时间
- [ ] **期望结果**：
  - 插件初始化时间在 500ms 以内
  - 不影响页面首屏加载
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

#### 10.2 列表渲染时间

- [ ] **操作步骤**：
  1. 准备 100 个量表数据
  2. 测量 `renderScaleList()` 的执行时间
- [ ] **期望结果**：
  - 渲染时间在 100ms 以内
  - 页面没有卡顿
- [ ] **实际结果**：****\_****
- [ ] **是否通过**：□ 是 □ 否

---

## 📊 测试结果汇总

### 统计数据

- **总测试项**：\_\_\_\_ 项
- **通过项**：\_\_\_\_ 项
- **失败项**：\_\_\_\_ 项
- **通过率**：\_\_\_\_%

### 失败项列表

| 测试项 | 失败原因 | 修复状态 |
| ------ | -------- | -------- |
|        |          |          |
|        |          |          |

---

## 🐛 发现的问题

### 问题1

- **问题描述**：
- **复现步骤**：
- **期望行为**：
- **实际行为**：
- **严重程度**：□ 低 □ 中 □ 高
- **建议修复方案**：

---

## ✅ 测试结论

- [ ] **通过** - 所有功能正常，可以上线
- [ ] **条件通过** - 有 minor 问题，但不影响核心功能，可以上线
- [ ] **不通过** - 有 major 问题，需要修复后才能上线

**测试人员签字**：****\_\_\_****  
**日期**：2026-06-01

---

## 📝 附录：测试环境信息

- **浏览器**：****\_\_\_****
- **浏览器版本**：****\_\_\_****
- **操作系统**：****\_\_\_****
- **屏幕分辨率**：****\_\_\_****
- **网络环境**：****\_\_\_****
- **插件版本**：scale-plugin.js v1.0.0
- **依赖版本**：
  - PluginBase: v1.0.0
  - PluginLoader: v1.0.3
  - DualAdapter: v1.0.0
  - EventHub: v1.0.0

---

**文档结束**

_本测试清单用于验证 scale-plugin.js 在实际页面环境中的功能完整性和稳定性。_
